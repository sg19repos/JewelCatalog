import React, { Fragment } from "react";
import FooterElements from "./footerElements";

const Footer = () => {
    return (
        <Fragment>
            <FooterElements />
        </Fragment>
    );
};

export default Footer;
